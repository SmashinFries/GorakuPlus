import { StyleSheet } from "react-native";

export const HeaderStyles = StyleSheet.create({
	icon: {
		borderRadius: 100,
		height: 42,
		width: 42,
		marginRight: 10,
		justifyContent: 'center',
		alignItems: 'center',
	},
});