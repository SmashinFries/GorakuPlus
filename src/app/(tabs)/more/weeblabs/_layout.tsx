import AnimatedStack from '@/components/stack';
import { Stack } from 'expo-router';

const WeebLabsPage = () => {
	return <AnimatedStack screenOptions={{ title: 'WeebLab' }} />;
};

export default WeebLabsPage;
