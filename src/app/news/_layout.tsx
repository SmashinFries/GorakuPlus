import AnimatedStack from '@/components/stack';

const NewsLayout = () => {
	return (
		<AnimatedStack
			screenOptions={{
				title: 'News',
			}}
		/>
	);
};

export default NewsLayout;
