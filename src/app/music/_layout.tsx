import AnimatedStack from '@/components/stack';

const MusicLayout = () => {
	return (
		<AnimatedStack
			screenOptions={{
				title: 'Music',
				headerShown: false,
			}}
		/>
	);
};

export default MusicLayout;
